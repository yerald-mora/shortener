@page
@model IndexModel
@{
    ViewData["Title"] = "Nativo Shortener";
}

<section class="pt-5 text-center container">
    <div class="row py-lg-2">
        <div class="col-md-10 mx-auto">
            <h1 class="fw-light">Nativo Shortener</h1>
            <p class="lead text-muted">
                You want to share a link among your friends in a simple way, or simply have a list of favorite articles on the internet,
                but you don't want to deal with so much text in these links... Nativo Shortener makes those details of your digital
                world.... simple 😎.
            </p>
        </div>
    </div>

    <div class="options py-lg-1">
        <ul class="nav nav-pills mb-3 d-flex justify-content-around" id="pills-tab" role="tablist">
            <li class="nav-item" role="presentation">
                <button class="nav-link active" id="pills-home-tab" data-bs-toggle="pill" data-bs-target="#pills-home" type="button" role="tab" aria-controls="pills-home" aria-selected="true">Shorten Url</button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" id="pills-profile-tab" data-bs-toggle="pill" data-bs-target="#pills-profile" type="button" role="tab" aria-controls="pills-profile" aria-selected="false">Top 20 Most Visited</button>
            </li>
        </ul>

        <div class="tab-content py-lg-1 col-md-10 mx-auto" id="pills-tabContent">
            <div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab">
                <form method="post">
                    <p class="lead text-muted">
                        Let's short that url ✂.
                    </p>

                    <div class="input-group mb-4">
                        <input id="txtUrl" placeholder="Paste your URL..." type="text" class="form-control">
                    </div>

                    <div class="input-group mb-4">
                        <input id="txtShortedUrl" placeholder="Shorted URL in 3, 2, 1... Just make the click" type="text" class="form-control">
                    </div>

                    <div class="d-flex justify-content-end">
                        <button type="button" id="btnShortenUrl" class="btn btn-success">Let's do this <i class="fa-solid fa-scissors"></i> </button>
                    </div>
                </form>
            </div>

            <div class="tab-pane fade" id="pills-profile" role="tabpanel" aria-labelledby="pills-profile-tab">
                
                <p class="lead text-muted">
                    Most visited 🔥
                    <button type="button" id="btnTop20MostVisited" class="btn btn-success"> <i class="fa-solid fa-arrows-rotate"></i></button>

                    <table class="table text-start" id="results">
                        <tr>
                            <th>URL</th>
                            <th>Visits</th>
                            <th>ShortCode</th>
                        </tr>

                    </table>

                </p>

            </div>

        </div>

    </div>
</section>

